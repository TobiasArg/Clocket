# Codex OpenSpec adapter

This directory contains the repository-scoped Codex skills for the OpenSpec
workflow. They are intentionally separate from `.opencode/skills/` only where
the command syntax of the host tool differs.

Shared, tool-agnostic skills belong in `.agents/skills/`.
