export default function Home() {
    return (
        <main style={{ fontFamily: 'Arial, sans-serif', padding: 24 }}>
            <h1>Clocket — Backend (Next.js)</h1>
            <p>Try the API at <code>/api/hello</code></p>
        </main>
    )
}
