# Kiro skill adapter

The entries in `skills/` are relative symbolic links to the canonical shared
skills in `.agents/skills/`. Keep links relative so the repository works after
cloning into a different path.

Do not add copied skill content or personal Kiro state to this directory.
