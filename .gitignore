# Node modules
node_modules/
**/node_modules/

# Build outputs
dist/
.next/

# Env files
.env
backend/.env
backend/.env.local
frontend/.env
frontend/.env.local

# OS files
.DS_Store

# Local agent/skill tooling artifacts
# Skills and Kiro adapters are versioned because they are part of the
# repository's reproducible agent workflow. Keep only generated state out.
skills-lock.json
